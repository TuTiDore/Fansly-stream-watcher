# Fansly Stream Watcher 👀

Automatically check when streamers you follow go live and download the stream directly to a file.

## Setup

Inside [environment file](.env), set environment variables

## Run

Run `docker compose up -d`
